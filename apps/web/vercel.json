{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Vercel deploy config. Mirrors apps/web/public/_redirects + _headers so the same dist/ output drops into Vercel too. Vercel picks vercel.json up when you point the project at apps/web as the root. If the deploy target is the repo root, move this to the repo root and add a rootDirectory field in the Vercel dashboard instead.",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    },
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/sw.js",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    }
  ]
}
