# Copy this file to `.codex/config.toml` and fill in local secrets there.
# Do not commit real API keys or per-user credentials.

[mcp_servers.stitch]
url = "https://stitch.googleapis.com/mcp"

[mcp_servers.stitch.http_headers]
X-Goog-Api-Key = "REPLACE_WITH_STITCH_GOOGLE_API_KEY"
